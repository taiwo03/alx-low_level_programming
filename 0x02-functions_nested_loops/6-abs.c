#include "main.h"
/**
 * _abs - computes the absolute value of an integrer
 *
 * @n: the integer to check
 *
 * Return:0 or 1
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

